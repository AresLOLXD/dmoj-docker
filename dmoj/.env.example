# Copy to dmoj/.env and uncomment on podman hosts only. Docker resolves the
# default (no localhost/ prefix, set in docker-compose.yml) without needing
# this file at all.
# RUNTIME_TIER3_IMAGE=localhost/areslolxd/runtimes-tier3:karel
